#include<stdio.h>
int main()
{
    int a;
    printf("Enter a \n");     //one liner if statement
    scanf("%d",&a);            // not use is large statement
    (a<5)? printf("A is less then 5"):printf("A is greater then 5");
return 0;
}